version: "3.8"

services:
  db:
    image: postgres:15
    container_name: go_pg_db
    environment:
      POSTGRES_USER: louay
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: sheet_db
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  pgdata:
